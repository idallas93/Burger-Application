<h1>Burgers!</h1>

<h2>Burgers that are not vegetarian!</h2>

<ul>
  {{#each burgers}}
    {{#unless veggie}}
      {{> burgers/burger-block veggie=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are vegetarian!</h2>

<ul>
  {{#each burgers}}
    {{#if veggie}}
      {{> burgers/burger-block veggie=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="veggie">Veggie?</label><br>
    <input type="radio" name="veggie" value="1" checked> Veggie option!<br>
    <input type="radio" name="veggie" value="0"> Meat!
  </div>

  <button type="submit">Add Burger</button>
</form>
